import 'package:flutter/material.dart';

class OffilineView extends StatelessWidget {
  const OffilineView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Offline'),
      ),
    );
  }
}
